# 快速优化清单 ⚡

本清单提供快速可执行的优化步骤，按优先级排序。

---

## 🔴 第一优先级（建议本周完成）

### ✅ 1. 删除冗余配置文件
```bash
# next.config.ts 是空文件，删除它
rm next.config.ts
```

### ✅ 2. 安装依赖包
```bash
npm install
```

### ✅ 3. 复用 Lightbox 组件
**文件**: `app/gallery/page.tsx`

删除165-222行的内联灯箱代码，替换为：
```typescript
import Lightbox from '../components/Lightbox';

// 删除内联的灯箱JSX，使用组件
<Lightbox 
  isOpen={lightbox.isOpen}
  imageSrc={lightbox.currentImage}
  onClose={closeLightbox}
/>
```

### ✅ 4. 添加错误边界
创建新文件 `app/components/ErrorBoundary.tsx`，内容见详细报告。

然后在 `app/layout.tsx` 中使用：
```typescript
import { ErrorBoundary } from './components/ErrorBoundary';

// 包裹 children
<ErrorBoundary>
  {children}
</ErrorBoundary>
```

---

## 🟡 第二优先级（建议2周内完成）

### ✅ 5. 图片格式优化

#### 方案A：使用在线工具批量压缩
1. 访问 [Squoosh.app](https://squoosh.app) 或 [TinyPNG](https://tinypng.com)
2. 批量上传照片
3. 选择WebP格式，质量80-85%
4. 下载并替换原文件

#### 方案B：使用脚本自动转换
创建 `scripts/convert-images.js`：
```javascript
const sharp = require('sharp');
const fs = require('fs');
const path = require('path');

const inputDir = './public/family-photos';
const outputDir = './public/family-photos-optimized';

// 确保输出目录存在
if (!fs.existsSync(outputDir)) {
  fs.mkdirSync(outputDir, { recursive: true });
}

// 读取所有图片文件
const files = fs.readdirSync(inputDir);

files.forEach(async (file) => {
  const ext = path.extname(file).toLowerCase();
  if (['.jpg', '.jpeg', '.png'].includes(ext)) {
    const inputPath = path.join(inputDir, file);
    const outputPath = path.join(outputDir, file.replace(/\.(jpg|jpeg|png)$/i, '.webp'));
    
    try {
      await sharp(inputPath)
        .webp({ quality: 80 })
        .toFile(outputPath);
      
      console.log(`✓ 已转换: ${file}`);
    } catch (error) {
      console.error(`✗ 转换失败: ${file}`, error);
    }
  }
});
```

安装依赖并运行：
```bash
npm install -D sharp
node scripts/convert-images.js
```

### ✅ 6. 添加 Web Vitals 监控
创建 `app/components/WebVitalsReporter.tsx`：
```typescript
'use client';

import { useEffect } from 'react';

export function WebVitalsReporter() {
  useEffect(() => {
    if (typeof window !== 'undefined') {
      import('web-vitals').then(({ onCLS, onFID, onFCP, onLCP, onTTFB }) => {
        onCLS(console.log);
        onFID(console.log);
        onFCP(console.log);
        onLCP(console.log);
        onTTFB(console.log);
      });
    }
  }, []);
  
  return null;
}
```

在 `app/layout.tsx` 中添加：
```typescript
import { WebVitalsReporter } from './components/WebVitalsReporter';

// 添加到 body 中
<WebVitalsReporter />
```

### ✅ 7. 启用主题切换功能
在 `app/page.tsx` 中添加：
```typescript
import ThemeToggle from './components/ThemeToggle';

// 在 LanguageSwitcher 旁边添加
<div style={{ position: 'fixed', top: '2rem', right: '6rem' }}>
  <ThemeToggle />
</div>
```

---

## 🟢 第三优先级（可选，1个月内）

### ✅ 8. 添加骨架屏
创建 `app/components/PhotoSkeleton.tsx`：
```typescript
export function PhotoSkeleton() {
  return (
    <div style={{
      height: '250px',
      backgroundColor: '#e0e0e0',
      borderRadius: '4px',
      animation: 'pulse 1.5s ease-in-out infinite'
    }} />
  );
}
```

添加 CSS 动画到 `app/globals.css`：
```css
@keyframes pulse {
  0%, 100% { opacity: 1; }
  50% { opacity: 0.5; }
}
```

### ✅ 9. 添加单元测试
安装测试依赖：
```bash
npm install -D @testing-library/react @testing-library/jest-dom jest jest-environment-jsdom
```

创建 `jest.config.js`：
```javascript
const nextJest = require('next/jest');

const createJestConfig = nextJest({
  dir: './',
});

const customJestConfig = {
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testEnvironment: 'jest-environment-jsdom',
};

module.exports = createJestConfig(customJestConfig);
```

创建测试文件示例 `app/utils/imageBlurData.test.ts`：
```typescript
import { blurDataURL } from './imageBlurData';

describe('imageBlurData', () => {
  it('应该返回有效的 data URL', () => {
    expect(blurDataURL).toMatch(/^data:image\/svg\+xml/);
  });
});
```

### ✅ 10. 添加 sitemap 和 robots.txt
创建 `app/sitemap.ts`：
```typescript
export default function sitemap() {
  return [
    {
      url: 'https://huang.co',
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 1,
    },
    {
      url: 'https://huang.co/gallery',
      lastModified: new Date(),
      changeFrequency: 'weekly',
      priority: 0.8,
    },
  ];
}
```

创建 `app/robots.ts`：
```typescript
export default function robots() {
  return {
    rules: {
      userAgent: '*',
      allow: '/',
      disallow: '/api/',
    },
    sitemap: 'https://huang.co/sitemap.xml',
  };
}
```

---

## 📊 优化效果验证

完成优化后，使用以下工具验证：

1. **Lighthouse**（Chrome DevTools）
   - 目标：Performance > 95
   
2. **WebPageTest** (https://www.webpagetest.org)
   - 测试不同地区的加载速度
   
3. **Google PageSpeed Insights** (https://pagespeed.web.dev)
   - 检查 Core Web Vitals
   
4. **Bundle Analyzer**
   ```bash
   npm install -D @next/bundle-analyzer
   ```

---

## 🎯 完成检查表

### 第一周
- [ ] 删除 next.config.ts
- [ ] 安装依赖 npm install
- [ ] 复用 Lightbox 组件
- [ ] 添加 ErrorBoundary

### 第二周
- [ ] 批量压缩图片
- [ ] 添加 Web Vitals 监控
- [ ] 启用主题切换
- [ ] 测试性能改进

### 第三周及以后
- [ ] 添加骨架屏
- [ ] 编写单元测试
- [ ] 添加 sitemap/robots
- [ ] 性能监控分析

---

## 💡 注意事项

1. **图片备份**：转换图片前，请备份原始照片
2. **测试环境**：先在开发环境测试所有改动
3. **性能监控**：部署后持续监控 Web Vitals 指标
4. **逐步实施**：不要一次性完成所有优化，逐步验证效果

---

**创建时间**: 2025-11-07  
**配套文档**: 项目优化分析报告_2025.md
